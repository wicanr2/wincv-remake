# 測試用壓縮檔

| 檔案 | 來源 | 內容 |
|---|---|---|
| `sample.7z` | 本專案自行產生(`7z a`) | `top.txt`、`docs/deep.txt` |
| `sample.rar` | 1999 年的 EOB3FIX 修正檔,取自本機既有素材 | `EOB3FIX.DOC`、`EOB3FIX.EXE` |

`sample.rar` 是真實世界的 RAR 而不是自己造的 —— 本機沒有 RAR 編碼器,
而用真檔測試反而比造出來的更有代表性(舊版 RAR、solid 壓縮)。
